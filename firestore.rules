rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Minden dokumentumhoz hozzáférés engedélyezése (fejlesztési célokra)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}